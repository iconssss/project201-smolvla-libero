# Checkpoint Manifest Template

- checkpoint ID / step:
- SHA256:
- model / dataset / code revisions:
- command and git commit:
- model config and processor hashes:
- optimizer / scheduler / RNG state present:
- reload-smoke timestamp and result:
- durable remote path and local-backup path:
