# Checkpoints

No checkpoint is stored in Git. Each remote checkpoint must have a local manifest with immutable input revisions, full config, processor/normalizer files, optimizer/scheduler state, RNG/meta state, SHA256, creation timestamps, and verified reload result.
