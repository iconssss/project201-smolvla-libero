# Environment Evidence

`stack_lock.json` was created during PRE-GPU from official metadata. R0/R1 will add `system_manifest.txt`, `pip_freeze.txt`, and an installation verification addendum; they may not alter the lock. No environment has been created during PRE-GPU.
