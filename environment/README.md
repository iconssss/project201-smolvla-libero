# Environment Evidence

`stack_lock.json` was created during PRE-GPU from official metadata. The primary LeRobot source baseline is the immutable current-main snapshot `3f2c29ef7e44b1ddccbcda3b6a63939e53639e9e`; stable `v0.6.1` is fallback/reference only. R0/R1 will add `system_manifest.txt`, `pip_freeze.txt`, and an installation verification addendum; they may not alter the lock. No environment has been created during PRE-GPU.
